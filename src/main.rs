mod lib;
/// Subjects: Ownership, Lifetime
/// In the lib file we find lifetime
/// Serialize/Deserialize
fn main() {
    println!(
        r#"
        Hello to you! 
        Check out lib.rs for what we looked at!
        "#
    );
}
